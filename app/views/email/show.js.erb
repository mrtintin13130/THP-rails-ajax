$(document).ready(function() {
/*
let new_email = $("<%= j (render 'email/email', email: @email) %>");
// Je crée ma variable new_email. Le j signifie escape javascript et permet d'executer de l'erb.

$('td').append(new_email);
*/

});
